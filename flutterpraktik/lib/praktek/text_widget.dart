import 'package:flutter/material.dart';

class text_widget1 extends StatelessWidget {
  const text_widget1({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Text('this is text widget'),
    );
  }
}